/// <reference types="vite/client" />

type StepMessageProps = {
  step: number;
  children: ReactNode;
};

type ButtonProps = {
  textColor: string;
  bgColor: string;
  onClick: () => void;
  children: ReactNode;
};

type StepMessageProps = {
  step: number;
  children: React.ReactNode;
};

type ButtonProps = {
  textColor: string;
  bgColor: string;
  onClick: () => void;
  children: React.ReactNode;
};